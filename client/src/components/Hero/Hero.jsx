import React from 'react';
import heroImg from "../../images/thrift-image1.png";

const Hero = () => {
    return (
        <section class="hero is-medium hero-img">
        <div class="hero-body hero-block">
          <p class="title hero-title">Thrift Shop²</p>
          <p class="subtitle hero-subtitle">Browse at your leisure!</p>
        </div>
      </section>
    );
};

export default Hero;